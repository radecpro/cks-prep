# Mixed scenarios

Planned. Add numbered scenario directories after focused labs are working.
Each scenario needs `TASK.md`, `setup.sh`, `verify.sh`, an isolated resource scope and explicit cleanup. Mix domains without exposing solutions or vulnerability locations in the task text.
