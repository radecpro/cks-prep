# CIS benchmark assessment

Status: planned — no runnable exercise yet.

Practice scope: Review benchmark findings, applicability, remediation and evidence.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-08` where applicable; document cluster-wide or host changes separately.
