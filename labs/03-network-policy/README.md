# NetworkPolicy

Status: planned — no runnable exercise yet.

Practice scope: Default deny, DNS, namespace selectors, ingress/egress, allowed and denied traffic probes.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-03` where applicable; document cluster-wide or host changes separately.
