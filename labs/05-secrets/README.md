# Secrets and sensitive data

Status: planned — no runnable exercise yet.

Practice scope: Secret access, workload exposure, encryption at rest and verification.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-05` where applicable; document cluster-wide or host changes separately.
