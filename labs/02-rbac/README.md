# RBAC and ServiceAccounts

Status: planned — no runnable exercise yet.

Practice scope: Least privilege, authorization checks, service account token exposure.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-02` where applicable; document cluster-wide or host changes separately.
