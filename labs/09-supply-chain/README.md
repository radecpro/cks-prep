# Supply chain security

Status: planned — no runnable exercise yet.

Practice scope: Image scanning, image identity, build minimization, SBOMs and admission controls.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-09` where applicable; document cluster-wide or host changes separately.
