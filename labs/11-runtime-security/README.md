# Runtime security

Status: planned — no runnable exercise yet.

Practice scope: Detection, investigation and containment; use Linux VMs for unsupported kernel features.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-11` where applicable; document cluster-wide or host changes separately.
