# Audit logging

Status: planned — no runnable exercise yet.

Practice scope: Audit policy, backend configuration and event verification.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-10` where applicable; document cluster-wide or host changes separately.
