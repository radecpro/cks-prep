# seccomp

Status: planned — no runnable exercise yet.

Practice scope: RuntimeDefault, custom profiles and workload behavior; check runtime support first.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-06` where applicable; document cluster-wide or host changes separately.
