# Control plane and kubelet hardening

Status: planned — no runnable exercise yet.

Practice scope: Configuration review, minimum exposure, safe rollback and health verification.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-07` where applicable; document cluster-wide or host changes separately.
