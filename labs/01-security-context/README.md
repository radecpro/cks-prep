# SecurityContext and capabilities

Status: planned — no runnable exercise yet.

Practice scope: Non-root execution, privilege escalation, capabilities, read-only filesystems, UID/GID.

When implementing, use `../../templates/TASK.md` and add `setup.sh` and `verify.sh`. Keep solutions and unsolicited hints out of the task. Use namespace `cks-lab-01` where applicable; document cluster-wide or host changes separately.
