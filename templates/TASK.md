# <Lab number>: <Title>

Status: planned
Time budget: <minutes>
Context: kind-cks
Namespace: cks-lab-<number>
Prerequisites: <cluster capabilities and tools>

## Scenario

<Operational context without revealing the solution.>

## Requirements

1. <Observable outcome and resource scope.>
2. <Availability/functionality that must be preserved.>

## Start

Run `./setup.sh`. Setup must state what it creates or resets and stop after preparing the initial environment.

## Verification

Run `./verify.sh`. Specify measurable pass criteria and any manual checks. Verification reports failures, returns nonzero if requirements fail and does not repair resources.

## Cleanup

<Explicit cleanup limited to this lab's resources.>
